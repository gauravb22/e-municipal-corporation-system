spring.datasource.url=jdbc:oracle:thin:@localhost:1521/XEPDB1
spring.datasource.username=GAURAV_APP
spring.datasource.password=gauravapp123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

spring.thymeleaf.cache=false

# Allow larger form posts for base64 complaint photos
server.tomcat.max-http-form-post-size=20MB
server.tomcat.max-swallow-size=20MB

# Uploads (completion photos, etc.)
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Keep users logged in for 7 days unless they logout
server.servlet.session.timeout=7d

# OTP is currently shown via local popup mode (no SMS provider)
